name: Deploy laravel Development
on: 
  push:
    branches: [ "main" ]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Build & Deploy App
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: 20.187.120.241
        username: vps1
        password: MagnumMax123
        port: 22
        script: |
          cd ~
          rm -rf KodeMaya
          rm -rf /var/www/html/KodeMaya
          git clone https://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@github.com/Erisukmawan/KodeMaya.git
          sudo cp -r KodeMaya /var/www/html
          cd /var/www/html/KodeMaya
          sudo mv .env.example .env
          sudo composer install
          sudo php artisan key:generate
          sudo systemctl reload apache2
          
